{% extends 'layout.html.twig' %}

{% block title %}
	Admin - Notre équipe -
	{{ parent() }}
{% endblock %}

{% block stylesheet %}<link rel="stylesheet" href="/assets/css/Admin/employee/add.css">
{% endblock %}

{% block content %}
	<main class="container">
		<div class="background-line"></div>
		<div class="main-title">
			<div class="title-content">
				<div class="bloc-square"></div>
				<h3>
					<span>Ajouter</span><br/>
					un(e) nouvel(le) employé(e)</h3>
			</div>
			<div class="second-title">
				<div class="bloc-rectangle"></div>
				<h4>à remplir</h4>
			</div>
		</div>
		<div class="formPage">
			<form {# action="/admin/notre-equipe" #} method="post" class="form">
				<div>
					<label for="lastname">Nom</label>
					<input type="text" name="lastname" id="lastname" value="{{ employe.lastname }}"/>
					<div class="error">
						{% if errors.lastname is not empty %}
							{{ errors.lastname }}
						{% endif %}
					</div>
				</div>
				<div>
					<label for="firstname">Prénom</label>
					<input type="text" name="firstname" id="firstname" value="{{ employe.firstname }}"/>
					<div class="error">
						{% if errors.firstname is not empty %}
							{{ errors.firstname }}
						{% endif %}
					</div>
				</div>
				<div>
					<label for="post">poste</label>
					<input type="text" name="post" id="post" value="{{ employe.post }}"/>
					<div class="error">
						{% if errors.post is not empty %}
							{{ errors.post }}
						{% endif %}
					</div>
				</div>
				<div>
					<label for="about">Sujet</label>
					<select name="about" id="about" placeholder="Type de demande">
						<option value="">département</option>
						{% for workDepartement in workDepartements %}
							<option value="{{ workDepartement.id }}">{{ workDepartement.name }}</option>
						{% endfor %}
					</select>
					<div class="error">
						{% if errors.about is not empty %}
							{{ errors.about }}
						{% endif %}
					</div>
				</div>
				<button>Enregistré</button>
			</form>
		</div>
		<div class="return">
			<a href="/admin/notre-equipe">retour</a>
		</div>
	</main>
{% endblock %}
